import React from 'react';
import { MultiSelectDropdown } from '../MultiSelectDropdown';

const props = {
  items: [
    {
      id: 1,
      name: 'React',
    },
    {
      id: 2,
      name: 'Redux',
    },
    {
      id: 3,
      name: 'Nodejs',
    },
  ],
  itemName: 'name',
};

describe('Multiple Select Dropdown', () => {
  beforeEach(() => {
    cleanup();
  });
  it('should render without errors', () => {});
});
