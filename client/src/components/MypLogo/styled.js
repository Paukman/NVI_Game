import styled from 'styled-components';

export const StyledMypLogo = styled.div`
  display: block;
  border: none;
  width: auto;
  padding-left: 10px;
  border-left: solid 1px #eee;
  font-weight: lighter;
  position: relative;
  top: 4px;
`;
