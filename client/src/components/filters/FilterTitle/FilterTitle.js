import styled from 'styled-components';

const FilterTitle = styled.h3`
  font-size: 20px;
  font-weight: bold;
  margin: 0 0 30px 0;
`;

FilterTitle.displayName = 'FilterTitle';

export { FilterTitle };
